{
  "name": "yoga-workout-importer",
  "version": "0.10.0-beta.1",
  "private": true,
  "type": "module",
  "description": "Local-only Obsidian importer for yoga workout text copied from Telegram.",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "test": "node --test"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
